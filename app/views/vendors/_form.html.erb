<%= simple_form_for(@vendor) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :service %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
