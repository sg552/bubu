<%= form_for(@category) do |f| %>
  <div class="field">
    名字：<br/>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    描述：<br/>
    <%= f.text_area :description, :size => "60x4" %>
  </div>
  <%= render :partial => "layouts/submit_and_cancel" %>
<% end %>
